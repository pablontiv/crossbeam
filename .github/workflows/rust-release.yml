# .github/workflows/rust-release.yml
name: Rust Release

on:
  workflow_call:
    inputs:
      quality-gate-jobs:
        required: true
        type: string
        description: 'JSON array of job names to wait for'
      binary-name:
        required: true
        type: string
        description: 'Binary name for builds and smoke tests'
      platforms:
        required: false
        type: string
        default: 'linux'
        description: 'Comma-separated: linux, macos, windows'
      changelog-tool:
        required: false
        type: string
        default: 'generate-notes'
        description: 'git-cliff or generate-notes'
      static-target:
        required: false
        type: string
        default: 'x86_64-unknown-linux-musl'
        description: 'Static build target'
      graduation-threshold:
        required: false
        type: number
        default: 5
        description: 'Minor version threshold for auto-graduation to v1.0.0. 0 disables.'

permissions:
  contents: write
  id-token: write
  attestations: write

jobs:
  auto-tag:
    name: Auto-tag
    runs-on: ubuntu-latest
    if: github.ref == format('refs/heads/{0}', github.event.repository.default_branch)
    outputs:
      new_tag: ${{ steps.tag.outputs.new_tag }}
      created: ${{ steps.tag.outputs.created }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Compute and push tag
        id: tag
        env:
          GRADUATION_THRESHOLD: ${{ inputs.graduation-threshold }}
        run: |
          # Find latest semver tag
          LATEST=$(git tag -l 'v*' | sort -V | tail -1)
          if [[ -z "$LATEST" ]]; then
            LATEST="v0.0.0"
          fi

          # Parse version components
          MAJOR=$(echo "$LATEST" | sed 's/^v//' | cut -d. -f1)
          MINOR=$(echo "$LATEST" | sed 's/^v//' | cut -d. -f2)
          PATCH=$(echo "$LATEST" | sed 's/^v//' | cut -d. -f3)

          # Classify every commit since the last tag (a push may contain several
          # commits); the highest-impact type wins: breaking > feat > other.
          if [[ "$LATEST" == "v0.0.0" ]]; then
            RANGE="HEAD"
          else
            RANGE="${LATEST}..HEAD"
          fi
          SUBJECTS=$(git log --pretty=%s "$RANGE")
          BODIES=$(git log --pretty=%b "$RANGE")
          BREAKING=false
          TYPE=other

          # Check for breaking: ! in subject OR BREAKING CHANGE trailer in body
          if echo "$SUBJECTS" | grep -qE '^[a-z]+(\(.+\))?!:' || echo "$BODIES" | grep -qE 'BREAKING CHANGE'; then
            BREAKING=true
          fi

          if echo "$SUBJECTS" | grep -qE '^feat(\(.+\))?[!]?:'; then
            TYPE=feat
          fi

          # Apply bump table
          if [[ $MAJOR -eq 0 ]]; then
            if [[ $GRADUATION_THRESHOLD -gt 0 && $MINOR -ge $GRADUATION_THRESHOLD && ("$TYPE" == "feat" || "$BREAKING" == "true") ]]; then
              NEW_TAG="v1.0.0"
            elif [[ "$TYPE" == "feat" || "$BREAKING" == "true" ]]; then
              MINOR=$((MINOR + 1))
              PATCH=0
              NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
            else
              PATCH=$((PATCH + 1))
              NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
            fi
          else
            if [[ "$BREAKING" == "true" ]]; then
              MAJOR=$((MAJOR + 1))
              MINOR=0
              PATCH=0
              NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
            elif [[ "$TYPE" == "feat" ]]; then
              MINOR=$((MINOR + 1))
              PATCH=0
              NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
            else
              PATCH=$((PATCH + 1))
              NEW_TAG="v${MAJOR}.${MINOR}.${PATCH}"
            fi
          fi

          echo "Computed tag: $NEW_TAG (from $LATEST, type=$TYPE, breaking=$BREAKING)"

          # Concurrent guard
          if git ls-remote --tags origin | grep -q "refs/tags/${NEW_TAG}$"; then
            echo "Tag $NEW_TAG already exists on remote, skipping"
            echo "new_tag=$NEW_TAG" >> "$GITHUB_OUTPUT"
            echo "created=false" >> "$GITHUB_OUTPUT"
          else
            git tag "$NEW_TAG"
            git push origin "$NEW_TAG"
            echo "new_tag=$NEW_TAG" >> "$GITHUB_OUTPUT"
            echo "created=true" >> "$GITHUB_OUTPUT"
          fi

  release:
    name: Release (Linux)
    runs-on: ubuntu-latest
    needs: auto-tag
    if: needs.auto-tag.outputs.created == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: stable
          targets: ${{ inputs.static-target }}

      - name: Install build tools
        run: |
          cargo install cargo-auditable cargo-zigbuild
          pip install ziglang

      - name: Install git-cliff
        if: inputs.changelog-tool == 'git-cliff'
        run: cargo install git-cliff

      - name: Set release version
        run: echo "RELEASE_VERSION=${{ needs.auto-tag.outputs.new_tag }}" >> "$GITHUB_ENV"

      - name: Build native
        run: cargo auditable build --release

      - name: Build static (musl)
        run: cargo auditable zigbuild --release --target ${{ inputs.static-target }}

      - name: Smoke test
        run: |
          chmod +x target/release/${{ inputs.binary-name }}
          target/release/${{ inputs.binary-name }} --version || target/release/${{ inputs.binary-name }} version || echo "Smoke test: binary executes"

      - name: Generate changelog
        id: changelog
        run: |
          if [[ "${{ inputs.changelog-tool }}" == "git-cliff" ]]; then
            git-cliff --latest --strip header > CHANGELOG.md
          else
            echo "Using GitHub auto-generated release notes"
            echo "" > CHANGELOG.md
          fi

      - name: Rename binaries with platform suffix
        run: |
          cp "target/release/${{ inputs.binary-name }}" "${{ inputs.binary-name }}-linux-x86_64"
          cp "target/${{ inputs.static-target }}/release/${{ inputs.binary-name }}" "${{ inputs.binary-name }}-linux-x86_64-musl"

      - name: Create GitHub release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          RELEASE_ARGS=()
          if [[ "${{ inputs.changelog-tool }}" != "git-cliff" ]]; then
            RELEASE_ARGS+=(--generate-notes)
          fi
          if [[ -s CHANGELOG.md ]]; then
            RELEASE_ARGS+=(--notes-file CHANGELOG.md)
          fi

          gh release create "${{ needs.auto-tag.outputs.new_tag }}" \
            --title "${{ needs.auto-tag.outputs.new_tag }}" \
            "${RELEASE_ARGS[@]}" \
            "${{ inputs.binary-name }}-linux-x86_64" \
            "${{ inputs.binary-name }}-linux-x86_64-musl"

      - name: Generate SLSA attestation
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8
        with:
          subject-path: '${{ inputs.binary-name }}-linux-x86_64'

  release-macos:
    name: Release (macOS)
    runs-on: macos-latest
    needs: [auto-tag, release]
    if: needs.auto-tag.outputs.created == 'true' && contains(inputs.platforms, 'macos')
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: stable
          targets: aarch64-apple-darwin

      - name: Install cargo-auditable
        run: cargo install cargo-auditable

      - name: Build
        run: cargo auditable build --release --target aarch64-apple-darwin

      - name: Rename binary with platform suffix
        run: cp "target/aarch64-apple-darwin/release/${{ inputs.binary-name }}" "${{ inputs.binary-name }}-macos-aarch64"

      - name: Upload to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.auto-tag.outputs.new_tag }}" \
            "${{ inputs.binary-name }}-macos-aarch64"

  release-windows:
    name: Release (Windows)
    runs-on: windows-latest
    needs: [auto-tag, release]
    if: needs.auto-tag.outputs.created == 'true' && contains(inputs.platforms, 'windows')
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@2c7215f132e9ebf062739d9130488b56d53c060c
        with:
          toolchain: stable

      - name: Install cargo-auditable
        run: cargo install cargo-auditable

      - name: Build
        run: cargo auditable build --release

      - name: Rename binary with platform suffix
        run: Copy-Item "target/release/${{ inputs.binary-name }}.exe" "${{ inputs.binary-name }}-windows-x86_64.exe"

      - name: Upload to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ needs.auto-tag.outputs.new_tag }}" `
            "${{ inputs.binary-name }}-windows-x86_64.exe"
