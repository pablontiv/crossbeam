---
tipo: session-state
proyecto: crossbeam
branch: master
fecha: "2026-03-27"
resumen: "Full crossbeam implementation + ecosystem migration complete. Next: move conform skill from praxis to crossbeam."
estado: saved
---

# Session Context

## Active Work

Implemented the full crossbeam shared infrastructure repo from spec to deployed — 8 reusable workflows, 6 configs, 9 templates, versioning strategy with aggressive semver + auto-graduation. Migrated all 7 consuming repos (rootline, localops, backscroll, kedral, homeserver, dotfiles, forger) to use crossbeam@v1 caller stubs. Added sops-guard workflow and deployed to all SOPS-using repos.

## Key Decisions

- Versioning: feat=minor even pre-1.0 (was patch), auto-graduation at threshold=5
- Migration: forward-only, no re-tagging existing versions. rootline got manual v1.0.0 tag
- crossbeam starts at v1.0.0 (stable from day 1), now at v1.2.0
- auto-tag.yml created for non-language repos (homeserver, dotfiles, forger)
- sops-guard.yml validates encrypted files without needing age key
- dotfiles gitleaks false positive resolved with .gitleaksignore
- Conform skill should move from praxis to crossbeam (next task)

## Modified Files

All clean and pushed. Key crossbeam files:
- `.github/workflows/` — codeql, scorecard, gitleaks, go-ci, rust-ci, go-release, rust-release, auto-tag, sops-guard (9 workflows)
- `configs/` — golangci, goreleaser, rustfmt, clippy, deny, editorconfig (6 configs)
- `templates/` — CONTRIBUTING, SECURITY, CoC, issues, PR, dependabot (9 templates)
- `scripts/test-auto-tag.sh` — versioning algorithm test (21/21 passing)
- `CLAUDE.md`, `README.md`

## Last Commit

930a6ae feat: add reusable sops-guard workflow — plaintext detection

## Next Steps

- Move /conform skill from praxis to crossbeam as native skill
- Refactor: eliminate bundled templates, use crossbeam's actual files as source of truth
- Rename from /conform to /crossbeam (or keep /conform as alias)
- Update checks to verify repos use crossbeam caller stubs
- The conform skill currently lives at /opt/praxis/.claude/skills/conform/
